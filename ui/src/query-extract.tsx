import React from 'react';
import ReactDOM from 'react-dom/client';
import QueryExtractPage from './pages/QueryExtractPage';
import './styles/index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <QueryExtractPage />
    </React.StrictMode>
);
